server:
  port: 8080
nacos:
  server-addr: http://nacos.stupidzhang.com:4848
  namespace: 52478819-0492-4091-9890-eca56e726d39
spring:
  application:
    name: cps-server
  cloud:
    nacos:
      discovery:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
      config:
        server-addr: ${nacos.server-addr}
        namespace: ${nacos.namespace}
        fileExtension: yaml
        shared-configs:
          - data-id: cps.yaml
            refresh: true
          - data-id: link-match.yaml
            refresh: true
